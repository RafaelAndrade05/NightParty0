<resources>

    <!-- Tema Claro -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- cambia el color de la status bar a gris -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Tema oscuro -->
    <style name="SecondActivityTheme.Base" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <!-- cambia el color de la status bar a gris -->
        <item name="colorPrimary">#000000</item> <!-- cambia action bar -->
        <item name="colorPrimaryDark">#000000</item> <!--color del status bar-->
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <!--Tema Activty detalles 2 -->
    <style name="DiseñoActivityDetalle2.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- cambia el color de la status bar a gris -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/my_ratingbar</item>
        <item name="android:minHeight">18dip</item>
        <item name="android:maxHeight">18dip</item>
    </style>

    <style name="TextDivider">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">4dp</item>
        <item name="android:layout_marginRight">4dp</item>
        <item name="android:textColor">#8B000000</item>
    </style>

    <style name="AppTheme.Base.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.Base.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
